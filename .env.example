# PostHog project API key. Public and write-only — safe to expose in client code.
# Leave unset locally: with no key, no analytics script is emitted at all.
# Use a PERSONAL project, not the Park one.
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
